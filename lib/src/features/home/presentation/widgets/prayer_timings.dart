import 'package:flutter/material.dart';

class PrayerTimings extends StatelessWidget {
  const PrayerTimings({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}